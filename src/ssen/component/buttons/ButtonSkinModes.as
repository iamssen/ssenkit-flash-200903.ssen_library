package ssen.component.buttons 
{
	import ssen.core.skinObject.SkinMode;	
	/**
	 * SkinMode 들 모음
	 * @author SSen
	 */
	public class ButtonSkinModes 
	{
		public static const MODES : Array = [SkinMode.DEFAULT, SkinMode.OVER, SkinMode.ACTION, SkinMode.DISABLE, SkinMode.SELECTED];
	}
}
